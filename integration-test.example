# Copy this file to .env.test.local on a development machine. This template is
# safe to commit because it targets only the local, disposable PostgreSQL database.
#
# The runner rejects cloud hosts and database names other than sparkle247_test.
TEST_DATABASE_URL="postgresql://ubuntu@localhost/sparkle247_test?host=/var/run/postgresql"
